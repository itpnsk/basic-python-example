python0:
  build:
    deploy: [ main.py ]
    addToRunPrepare: [ requirements.txt ]
  run:
    prepare:
        - python3 -m pip install -r requirements.txt
    start: python3 main.py
python1:
  build:
    deploy: [ main.py ]
    addToRunPrepare: [ requirements.txt ]
  run:
    prepare:
        - python3 -m pip install -r requirements.txt
    start: python3 main.py
python2:
  build:
    deploy: [ main.py ]
    addToRunPrepare: [ requirements.txt ]
  run:
    prepare:
        - python3 -m pip install -r requirements.txt
    start: python3 main.py
python3:
  build:
    deploy: [ main.py ]
    addToRunPrepare: [ requirements.txt ]
  run:
    prepare:
        - python3 -m pip install -r requirements.txt
    start: python3 main.py
